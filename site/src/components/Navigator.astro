---
const currentPath = Astro.url.pathname.replace(/\/$/, "") || "/";
console.log("Current path:", currentPath);
const backPath = currentPath.split("/").slice(0, -1).join("/") || "/";
---

<div class="text-lg font-semibold prose">
    <a href={backPath}>&lt;- [back]</a>
</div>
